function setup() {
  createCanvas(800, 600);
  background(51);
}

function draw() {
  // noStroke();
  stroke(255, 0, 100);

  noFill();
  //fill(255, 0, 100);

  // ellipse(mouseX, mouseY, 25, 25);
  ellipse(400, 300, 25, 25);
}
